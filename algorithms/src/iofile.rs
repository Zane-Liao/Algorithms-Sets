use std::fs::File;
use std::io::{self, Read, Result};

fn process_file() -> Result<()> {
    Ok(())
}

fn block_process() {
    
}

#[cfg(test)]
mod test {
    use super::*;

    #[test]
    fn test() {}
}